// GENERATED BY MBOSS — DO NOT EDIT.
// Regenerated from
// .mboss/workflows/timer_wait.workflow.json.

import { DBOS } from '@dbos-inc/dbos-sdk';

import { recordBooking } from '../../lib/recordBooking.js';
import type { Booking } from '../../lib/types.js';
import type {
  EventWait,
  PayloadCheck,
  TriggerDescriptor,
  WaitDescriptor,
} from '../app/contract.js';
import { appDb } from '../app/db.js';

async function timerWaitFn(evt: Booking): Promise<void> {
  // Durable: the wake-up time is written to the
  // database, so the wait survives a restart.
  // 900 seconds, as milliseconds.
  await DBOS.sleep(900000);

  const recordBookingOut = await appDb.runTransaction(
    async () => recordBooking(evt),
    { name: 'record_booking' },
  );
}

export const timerWait = DBOS.registerWorkflow(timerWaitFn, {
  name: 'timer_wait',
});

export const trigger: TriggerDescriptor = {
  mode: 'event',
  topic: 'booking.placed',
};

export function checkPayload(payload: unknown): PayloadCheck {
  if (typeof payload !== 'object' || payload === null) {
    return { ok: false, problem: 'the payload is not an object' };
  }
  return { ok: true, key: undefined, requesterEmail: undefined };
}

export const waits: Record<string, WaitDescriptor> = {};

export const eventWaits: EventWait[] = [];
